runtime:
  max_steps: 12
  default_mode: architect
  provider: stub

workspace:
  root: workspace

policy:
  default: deny
  shell_enabled: false
  authoritative_source: core/capability_policy.py

model_qualification:
  default_trust: observer
  require_profile: true
  authoritative_source: core/model_profile.py
